<Page x:Class="MultimediaLibrary.DisplayTrackPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MultimediaLibrary"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="DisplayTrackPage">

    <Grid>
        <Label  x:Name="artistNameLabel" FontSize="28" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="20,10,0,0" Foreground="#FF152B2B"/>
        <DataGrid x:Name="displayTracks" Background="#FF606793" Foreground="#FF152B2B" FontSize="16" AutoGenerateColumns="False" Margin="150,60,150,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Track" Width="400" Binding="{Binding Path=Name}"/>
                <DataGridTemplateColumn Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Open" Click="OpenTrackButton_Click"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
